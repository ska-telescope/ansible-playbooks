[Unit]
Description=Elasticsearch in Docker
After=docker.service
Requires=docker.service

[Service]
TimeoutStartSec=0
ExecStart=/usr/bin/docker start -a {{ elasticsearch_service }}
ExecStop=/usr/bin/docker stop -t 20 {{ elasticsearch_service }}

[Install]
WantedBy=multi-user.target
